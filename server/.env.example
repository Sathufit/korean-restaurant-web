
NODE_ENV=development

# Server Port
PORT=5001

# ============================================
# MONGODB CONFIGURATION
# ============================================

# Option 1: MongoDB Atlas (Cloud - Recommended for Production)
# Get your connection string from: https://cloud.mongodb.com
# Example: mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/hanguk_bites
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/hanguk_bites


JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_min_32_characters

# Generate a session secret with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your_session_secret_change_this_also

# ============================================
# CORS CONFIGURATION
# ============================================
# Comma-separated list of allowed origins
# Development:
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
# Production example:
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# RATE LIMITING
# ============================================
# Time window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Maximum number of requests per window
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# ADMIN CONFIGURATION
# ============================================
# Default admin email (for password reset, etc.)
ADMIN_EMAIL=admin@hangukbites.com

# ============================================
# EMAIL CONFIGURATION (Optional - for notifications)
# ============================================
# SMTP settings for sending emails
# For Gmail: Use App Password (not your regular password)
# https://support.google.com/accounts/answer/185833
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Email from address
EMAIL_FROM=noreply@hangukbites.com

# ============================================
# LOGGING
# ============================================
# Log level: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# ============================================
# SECURITY NOTES
# ============================================
# 1. Change ALL passwords and secrets above
# 2. Use strong passwords (min 16 characters)
# 3. Generate secrets using crypto.randomBytes()
# 4. Never expose this file publicly
# 5. Use different values for development and production
# 6. Rotate secrets regularly (every 3-6 months)
